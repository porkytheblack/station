# Station CLI execution coverage

Checked against the current [execution dispatcher](../packages/station-daemon/src/server/routes/execution.ts), [BrowserCommand union](../packages/station-browser-use/src/commands.ts), [client](../packages/station-client/src/index.ts) and [CLI dispatcher](../packages/station-cli/src/commands.ts). This maps available operations to commands and distinguishes transport tests from real backend acceptance. It does not claim every operation works on every provider. Run `node docs/scripts/check-cli-coverage.mjs` after changing either contract: it fails for missing method names, while the linked behavior tests remain separate.

Use `station sandbox METHOD [ID] --station OWNER --json @request.json` or `station browser METHOD [ID] --station OWNER --json @request.json`. JSON contains method-specific fields; `method` and `id` come from positional arguments. `ID` means the sandbox, session, recording, profile or checkpoint identifier required by that method. Both commands use the selected context and owner route; a tenant context uses the restricted tenant route. Authorization and advertised backend capabilities still apply. Mutations are not automatically retried.

## Sandbox methods

| Supported RPCs | CLI access | Evidence and its boundary |
| --- | --- | --- |
| `create`, `list`, `get`, `destroy` | Generic sandbox command above; omit ID for create/list. | [Daemon execution routes](../packages/station-daemon/test/server/routes/execution.test.ts) exercise authenticated owner routing and real command lifecycle; [client](../packages/station-client/test/client.test.ts) exercises tenant owner encoding. |
| `exec`, `command`, `cancel` | `sandbox exec ID --command 'git status'`; generic `command`/`cancel` with `{"runId":"..."}`. Exec accepts `cwd` and `timeoutMs` in JSON. | Daemon execution test exercises real host commands; [sandbox host tests](../packages/station-sandbox/test/host.test.ts) cover lifecycle. Default asynchronous API success is not remote process success: inspect status/exitCode or use `--wait`. [Wait tests](../packages/station-cli/test/command-wait.test.ts) cover remote status mapping, timeout, cancellation and owner/result validation. |
| `listFiles`, `readFile`, `writeFile`, `removeFile` | Generic method with path/options; `sandbox upload ID --file LOCAL --path REMOTE [--parents]`; `sandbox download ID --path REMOTE --out LOCAL`. | [CLI transfers](../packages/station-cli/test/transfers.test.ts) test byte pagination, incomplete cleanup and no overwrite; [client transfers](../packages/station-client/test/transfers.test.ts) test bytes, size/base64/offset validation; daemon advanced gateway test covers files. |
| `startService`, `services`, `service`, `stopService`, `restartService`, `removeService` | Generic method with `options` for start or `serviceId` for a service. | Daemon advanced sandbox gateway test and [sandbox advanced](../packages/station-sandbox/test/advanced.test.ts) exercise ownership/lifecycle. No separate bespoke CLI flag for every service field. |
| `openTerminal`, `terminals`, `terminal`, `terminalInput`, `resizeTerminal`, `closeTerminal` | Generic methods; interactive `sandbox shell ID [--cwd PATH]` or `sandbox attach ID --terminal TERMINAL_ID`, each with `--station OWNER`. | [Terminal tests](../packages/station-cli/test/terminal.test.ts) cover cursors, Unicode, Ctrl-C forwarding, resize, Ctrl-] detach, abort and raw-mode restoration; [real PTY smoke](../packages/station-cli/scripts/tui-pty-smoke.mjs) covers actual TUI terminal interaction. Detach leaves the remote terminal managed by its worker. |

The sandbox RPC currently has no arbitrary per-command environment override or VM snapshot/mount/boot operation. Shared tool installs and approved worker configuration remain server responsibilities. A generic API command cannot add an unsupported backend capability.

## Browser methods

| Supported RPCs or `execute` operations | CLI access | Evidence and its boundary |
| --- | --- | --- |
| `open`, `list`, `close` | Generic browser command; open accepts `options`, including a permitted `profileId`. | Daemon browser RPC lifecycle test; [manager](../packages/station-browser-use/test/manager.test.ts) tests and separate provider integration suites. |
| `action` | `browser action SESSION --json '{"action":"navigate","value":"https://example.com"}'`; screenshot helper below. | Daemon browser routes and Browser Use browser integration. Provider-specific actions remain capability checked. |
| `execute`: `fill`, `select`, `check`, `hover`, `click`, `focus`, `press`, `scroll`, `waitFor`, `mouseClick`, `mouseMove`, `drag`, `dragCoordinates`, `dialog` | `browser execute SESSION --json @command.json`, containing `{"command":{"op":"...",...}}`. Use structured target/selector fields accepted by BrowserCommand. | [Command validation](../packages/station-browser-use/test/commands.test.ts), [interaction integration](../packages/station-browser-use/test/interaction.integration.ts), daemon advanced browser validation. Generic forwarding is shared, not a fresh CLI test per gesture/provider. |
| `execute`: `content`, `back`, `forward`, `reload`, `pages`, `newPage`, `selectPage`, `closePage`, `inspect`, `accessibility` | Same `browser execute` form. | Browser command/interaction/advanced suites and daemon dispatch validation; backend support varies. |
| `execute`: `upload`, `download`, `downloadRead`, `downloadDelete`; `action` screenshot | `browser upload SESSION --file LOCAL --selector CSS [--mime TYPE]`; initiate download with `execute`/`download`; retrieve with `browser download SESSION --artifact ARTIFACT --out LOCAL`; `browser screenshot SESSION --out LOCAL`. | CLI/client transfer tests cover local bytes, output creation and control token; separate browser integration covers actual file input/download. Inline upload limit is 4 MiB decoded. Provider limitations are not hidden by CLI helpers. |
| `execute`: `diagnostics`, `traceStart`, `traceStop`; `audit` | Generic `execute` or `browser audit`. | [Trace tests](../packages/station-browser-use/test/trace.test.ts), command tests and browser advanced integration. Trace start/stop is not a promise of a trace archive export API on every backend. |
| `profiles`, `profileDelete` | Generic browser methods with profile ID for delete. | Browser manager/durable/remote tests cover configured profile behavior; local persistence and remote provider profiles have different guarantees. |
| `control`, `controlAcquire`, `controlRenew`, `controlRelease`, `liveFrame` | Generic browser methods; JSON fields include `ttlMs` and/or `controlToken`; supported mutation commands also accept `--control-token`. | [Control state](../packages/station-browser-use/test/control-state.test.ts) and browser interaction tests establish lease semantics. A frame result is not automatically a graphical terminal viewer. |
| `checkpoints`, `checkpoint`, `checkpointDelete`, `checkpointResume` | Generic browser methods; checkpoint uses session ID, delete/resume use checkpoint ID. | [Durable tests](../packages/station-browser-use/test/durable.test.ts) cover recovery semantics. Restored checkpoints are not live process snapshots. |
| `recordingStart`, `recordingStop`, `recordings`, `recording`, `recordingFrame`, `recordingDelete` | Generic browser methods; recordingFrame includes `frameId`. | Daemon owner-routed recording test and [recording tests](../packages/station-browser-use/test/recording.test.ts). CLI returns structured frame data; dashboard provides visual playback. |

## Cross-cutting evidence and limits

- `sandbox stations` and `browser stations` discover execution owners. Daemon discovery tests cover capabilities, online status and admin authentication; routing never relocates an existing session to another worker.
- The [client suite](../packages/station-client/test/client.test.ts) tests API/identity handshake, remote TLS, origin boundaries, sanitized failures, response bounds and no automatic mutation retry. [Tenant route tests](../packages/station-daemon/test/server/routes/tenant-execution.test.ts) test verified identity at both boundaries, unsafe backend rejection, quotas and durable browser prerequisites. These are meaningful boundary tests, not an exhaustive tenant denial test for every row.
- [Live stream tests](../packages/station-cli/test/live.test.ts) exercise cursor reconnect and reset handling; [daemon SSE tests](../packages/station-daemon/test/server/sse.test.ts) cover bounded replay. Replay is process-local, not a durable audit log.
- For scripts, `station sandbox exec ID --station OWNER --command "git status" --wait --wait-timeout-ms 300000 --json-errors` waits for the accepted command and prints the final JSON result. Ordinary remote exit codes are preserved; remote timeout maps to 124, cancellation to 130 and interrupted/lost execution to 125. A failed result with no usable exit code maps to 1. Default exec remains asynchronous. The local wait deadline is 300000 ms by default (1–86400000 allowed), starts after the execution receipt, and stops polling without cancelling the remote command; Ctrl-C likewise detaches the waiter. Local timeout/interrupt returns the latest known JSON snapshot, exits 124/130 and reports `wait_timeout`/`wait_interrupted`. Set a remote execution timeout separately using `--json '{"timeoutMs":10000}'`. Global `--json-errors` emits a sanitized `{error:{code,status,message}}` on stderr; transport failures exit 1 and are distinct from the returned remote result. Neither polling failure nor disconnect replays exec or sends cancel.
- Not every provider/OS/tenant combination has a recorded end-to-end CLI test for every operation. Actual container/browser acceptance suites establish their configured tested backends only. No VM backend is advertised.

For release evidence and the remaining explicitly required acceptance work, see [the acceptance map](STATION-IMAGES-ACCEPTANCE.md).
